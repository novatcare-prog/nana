name: mch_core
description: Core domain models and business logic for MCH Kenya
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.5.1

  # Annotations for code generation
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  
  # Backend
  supabase_flutter: ^2.0.0 

  # --- ADDED: Offline Database ---
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # --- ADDED: ID Generation ---
  uuid: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  freezed: ^2.4.5
  json_serializable: ^6.7.1
  flutter_lints: ^3.0.0

  # --- ADDED: Hive Code Generator ---
  hive_generator: ^2.0.1

flutter: