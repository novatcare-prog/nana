name: mch_core
description: Core domain models and business logic for MCH Kenya
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management (Required for the Providers in your repositories)
  flutter_riverpod: ^2.5.1

  # Annotations for code generation
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  
  # Backend
  supabase_flutter: ^2.0.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  freezed: ^2.4.5
  json_serializable: ^6.7.1
  flutter_lints: ^3.0.0

flutter: