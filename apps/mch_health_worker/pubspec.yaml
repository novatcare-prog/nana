name: mch_health_worker
description: MCH Health Worker Application for clinic staff
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  mch_core:
    path: ../../packages/mch_core
  
  # Cross-platform SQLite (will remove later)
  sqflite_common_ffi: ^2.3.0
  sqflite: ^2.3.0
  sqflite_common_ffi_web: ^0.4.0
  path: ^1.8.3
  path_provider: ^2.1.1
  
  # Supabase
  supabase_flutter: ^2.0.0
  internet_connection_checker_plus: ^2.1.0
  
  # Environment variables
  flutter_dotenv: ^5.1.0
  
  # REMOVED FOR NOW - we'll add back when needed:
  # flutter_secure_storage: ^9.0.0
  # shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - .env